/**
 * ============================================
 * GMS - Dashboard Store (DEPRECATED)
 * ============================================
 * Unused. The dashboard stats are requested directly in views/Dashboard.vue
 * via dashboardService.getStats().
 * ============================================
 */

export const useDashboardStore = () => {
  return {};
};
