/**
 * ============================================
 * GMS - Profile Store (DEPRECATED)
 * ============================================
 * Unused. Profile management is handled directly via authStore and /auth/profile endpoints.
 * ============================================
 */

export const useProfileStore = () => {
  return {};
};
