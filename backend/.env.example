# Database Connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gms?schema=public
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gms

# App Port
PORT=3001

# Environment mode (development / production)
NODE_ENV=development

# CORS setting, required for frontend communication
CORS_ORIGIN=http://localhost:8081

# JWT Settings - MUST BE AT LEAST 32 CHARACTERS IN PRODUCTION!
JWT_ACCESS_SECRET="generate_at_least_32_chars_random_hex_for_access_secret_key_gms"
JWT_REFRESH_SECRET="generate_at_least_32_chars_random_hex_for_refresh_secret_key_gms"
JWT_ACCESS_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# File Upload Settings
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE_MB=10

# Default Seed Passwords
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_QC_PASSWORD=qc123
DEFAULT_WAREHOUSE_PASSWORD=warehouse123
DEFAULT_SECURITY_PASSWORD=security123
